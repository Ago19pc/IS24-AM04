package main.java.Server.Client;

import java.net.Socket;

/**
 * The Client class
 * This is a jotting with no sense, so it has to be remade completely
 */
public class Client {
    Socket socket;
    String ipv4;

    public Client(String ipv4) {
        this.ipv4 = ipv4;
    }
}
